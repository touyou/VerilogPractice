8bitを送信する手順（送信側）
通信路をH状態に保持しているのが、送信していない状態。
送信開始。通信路をL状態にしdtだけ保持する。これをstart bitと呼ぶ。
以下を i = 0からi = 7まで繰り返す
sdata[i] = 0なら通信路をL状態にし、sdata[i] = 1なら通信路をH状態にする。
dtだけ通信路の状態を保持する。
通信路をH状態に設定しdtだけ保持する。これをstop bitと呼ぶ。
手順1に戻る。


8bitを受信する手順（受信側）
データを待っている状態では通信路の状態変化をdtよりも十分に短い時間間隔で観測している。
start bitによって通信路の状態がHからLに変化することで、送信が始まったことを認識し時間dt/2だけ待つ。以後の通信路の観測を状態が安定している時刻に行いたいのでdt/2が良い時間である。
以下を i = 0からi = 7まで繰り返す
時間dtだけ待つ。
通信路の状態を観測し結果がHであればrdata[i] = 1, Lであればrdata[i] = 0と記録する。記録は実際にはシフトレジスタにすると良い。
時間dtだけ待つ。
通信路の状態を観測し、状態がstop bitによってHとなることを確認して、正しい個数のデータを受け取ったことを認識する。ここでL状態を観測した場合には、それをフレーミング・エラーとして検出する。
手順1に戻る。
